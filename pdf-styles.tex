\usepackage{fontspec}

\setmainfont{LibertinusSerif}[
    Path = fonts/,
    Extension = .ttf,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
    BoldItalicFont = *-BoldItalic
]

\setsansfont{LibertinusSans}[
    Path = fonts/,
    Extension = .ttf,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
]

\usepackage{setspace}
% Set leading to 1.2
\setstretch{1.2}

\usepackage[automark]{scrlayer-scrpage}

% Remove %s on next two lines to force headers and sans-serif elements to use the main font family
% \renewcommand{\familydefault}{\rmdefault}
% \setkomafont{disposition}{\rmfamily\bfseries}

% Set font size to 9/12pt and remove default KOMA italics
\setkomafont{pagehead}{\fontsize{9}{12}\normalfont\selectfont}

% Header: Centered, Tracked (+50), All Caps
\makeatletter
\chead{\textls[50]{\MakeUppercase{\@title\ \textendash\ \headmark}}}
\makeatother

\pagestyle{scrheadings}

% \deffootnote[fn num box width]{1st para indent}{subsequent para w/in fn indent}{footnote number format, and specifying it this way makes it plain text instead of superscript}
% okay hanging option \deffootnote[1.5em]{1.5em}{1em}{%
%   \makebox[1.5em][l]{\thefootnotemark.\ }% 
% }

% --- KOMA-Script: True Flush-Left, Run-in Footnotes ---
% We set the 'width' and 'indent' to 0pt to keep everything flush.
% The magic is in the negative \hspace to pull the mark back inside.
\deffootnote[0pt]{0pt}{1em}{%
  \hspace{0pt}\thefootnotemark.\ %
}

% Adds half a line of space between separate footnotes
\addtolength{\footnotesep}{0.5\baselineskip}
