\usepackage{fontspec}

\setmainfont{LibertinusSerif}[
    Path = fonts/,
    Extension = .ttf,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
    BoldItalicFont = *-BoldItalic
]

\usepackage[automark]{scrlayer-scrpage}

% Force headers and sans-serif elements to use the main font family
\renewcommand{\familydefault}{\rmdefault}
\setkomafont{disposition}{\rmfamily\bfseries}

% Set font size to 9/12pt and remove default KOMA italics
\setkomafont{pagehead}{\fontsize{9}{12}\normalfont\selectfont}

% Header: Centered, Tracked (+50), All Caps
\makeatletter
\chead{\textls[50]{\MakeUppercase{\@title\ \textendash\ \headmark}}}
\makeatother

\pagestyle{scrheadings}
